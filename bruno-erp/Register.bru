meta {
  name: Register
  type: http
  seq: 2
}

post {
  url: {{API_URL}}/register
  body: json
  auth: inherit
}

body:json {
  {
    "name": "CONNECT",
    "description": "Connect everything",
    "version": "1.0.0",
    "routes": [
      {
        "path": "ping",
        "method": "GET",
        "permission": 0
      }
    ],
  //   "overrideIp": "host.docker.internal",
    "listeningPort": 8000,
    "apiKey": "{{API_KEY}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
